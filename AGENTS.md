# Agent notes — OLTC Selector

## Icons

Do not invent or hand-draw icons. Use [@heroicons/react](https://heroicons.com/). Tab favicon is the ⚡ emoji SVG.

## CLI

`npx -y mr-oltc-selector@1.0.0`. Same engine as the web app. No prices. Bin is `mr-oltc`. `--iu` is Imax and is never multiplied by `--k`.

## Ranking

Minimum-adequate: VI / VV compact vacuum, then VM, then VRS / VRL. OILTAP V / G only on oil. Combined III is Y-only; delta uses 3× I when that row exists.

## Type string

`VV III 250 Y-76-10 19 3W` (compound, no selector letter).
`VM III 500 Y-123/B-10 19 3W` (combined, selector letter).

## Website copy

No standalone MR token in visible chrome. Repo slug `/mr-oltc-selector/` is allowed.
